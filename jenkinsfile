pipeline {
  agent {
    docker { image 'node:17-alpine' }
  }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World' "Hello jenkins"
                sh 'node --version'
      }
    }
  }
}
